import { State } from "type";
import { css } from "@emotion/css/macro";

const cssFrame = () => {
  const masterMain = (props?: State) => css``;

  return {
    masterMain,
  };
};

export default cssFrame;
